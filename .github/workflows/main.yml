name: es-check-action
on: [push]
jobs:
  es-check-action-job:
    runs-on: windows-2016
    steps:
      - uses: ali-kamalizade/es-check-action@1.0.2
        with:
          directory: '../../lib'
          ecmaVersion: 'es3'
          files: 'index.js'